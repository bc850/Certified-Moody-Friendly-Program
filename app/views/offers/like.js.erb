$("#like_<%= @offer.id %>").removeClass('glyphicon-heart-empty').addClass('glyphicon-heart');
$("#like_<%= @offer.id %>").attr("href", '/offers/<%= @offer.id %>/unlike');
$("#popularityRender").html("<%= j (render partial: 'feed/popularity_component') %>");
$(".favsPartial").html("<%= j render(partial: 'feed/favs_partial') %><%= j render(partial: 'feed/side_bar_advertisements') %>");
$('.likes-count').html("<%= @offer.get_upvotes.size %>");
