<div id="favorite">
  <% @side_bar_advertisement.each do |advertisement| %>
    <%= link_to image_tag(advertisement.image_url, width: 250) %>
    <br>
    <p>
      <%= advertisement.description %>
    </p>
  <% end %>
</div>
