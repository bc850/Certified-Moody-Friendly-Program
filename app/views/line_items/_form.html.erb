<%= form_with(model: line_item, local: true) do |form| %>
  <% if line_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_item.errors.count, "error") %> prohibited this line_item from being saved:</h2>

      <ul>
      <% line_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :coupon_id %>
    <%= form.text_field :coupon_id, id: :line_item_coupon_id %>
  </div>

  <div class="field">
    <%= form.label :discount_id %>
    <%= form.text_field :discount_id, id: :line_item_discount_id %>
  </div>

  <div class="field">
    <%= form.label :event_id %>
    <%= form.text_field :event_id, id: :line_item_event_id %>
  </div>

  <div class="field">
    <%= form.label :favorite_id %>
    <%= form.text_field :favorite_id, id: :line_item_favorite_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
