$("#feedID").html("<%= j (render partial: 'feed/feed_partial') %>");

$("#notification").fadeIn("slow").append('Thank you. This listing has been reported to the administrators and hidden from your feed.');
$(".dismiss").click(function(){
       $("#notification").fadeOut("slow");
});
