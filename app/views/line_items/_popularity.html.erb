<div id="popularityDiv">
  <% @popularity.each do |offer| %>
    <h5><%= offer.name %></h5>
    <p id="popularityParagraph">
      <span id="redColorSpan"><%= offer.popularity %></span>
      favorites for this
      <%= offer.offering_type %>!</p>
  <% end %>
</div>
